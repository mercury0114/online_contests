#pragma once

#include <algorithm>
#include <deque>
#include <limits>
#include <map>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <vector>

using std::deque;
using std::make_pair;
using std::max;
using std::map;
using std::min;
using std::numeric_limits;
using std::pair;
using std::string;
using std::vector;
using std::unordered_map;
using std::unordered_set;

typedef unsigned int UI;
typedef long long LL;
typedef pair<LL, LL> PLL;
typedef vector<LL> VLL;
typedef vector<VLL> VVLL;
typedef vector<bool> VB;
typedef vector<std::string> VS;

constexpr LL MAX_LL = numeric_limits<LL>::max();
constexpr LL MIN_LL = numeric_limits<LL>::min();
